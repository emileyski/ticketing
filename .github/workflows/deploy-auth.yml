name: auth deploy

on:
  push:
    branches: [master]
    # paths:
    #   - "auth/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install IBM CLI
        run: curl -fsSL https://clis.cloud.ibm.com/install/linux | sh

      - name: Login to IBM with API_KEY
        run: ibmcloud login --apikey $IBM_CLOUD_API_KEY -r eu-de
        env:
          IBM_CLOUD_API_KEY: ${{secrets.IBM_CLOUD_API_KEY}}

      - name: Make Docker image
        run: |
          cd auth
          docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
          docker build -t emilevi4/auth .
        #   docker tag emilevi4/auth de.icr.io/ticketing/auth
        #   docker push de.icr.io/ticketing/auth
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
